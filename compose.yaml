services:
  ai:
    build: .
    command: uvicorn main:app --host 0.0.0.0 --port 8000 --reload
    env_file:
      - .env
    volumes:
      - .:/app
    ports:
      - "127.0.0.1:8001:8000"
    networks:
      - fitness-tracker-network

networks:
  fitness-tracker-network:
    external: true